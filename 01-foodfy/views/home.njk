{% extends "layout.njk" %}

{% block content %}

<section class="welcome">
    <h1>As melhores receitas</h1>
    <p>Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
    <img src="/assets/chef.png" alt="Mascote do Foodfy">
</section>

<section id="receitas">
    <h2>Mais acessadas</h2>
    <div class="receitas-container">
        {% for receita in receitas %}
            {% if receita.destaque %}
            <article class="receita-card">
                <img src="{{ receita.url }}" alt="Foto de {{ receita.nome }}">
                <h3>{{ receita.nome }}</h3>
                <h4>por {{ receita.autor }}</h4>
            </article>
            {% endif %}
        {% endfor %}
    </div>
</section>

<div class="modal-overlay">
    <div class="modal-container">
        <div class="modal-card">
            <img src="" alt="Foto da receita">
            <h2>Nome da receita</h2>
            <h3>por Autor</h3>
            <a src="" id="close-modal">Fechar</a>
        </div>
    </div>
</div>

{% endblock %}}